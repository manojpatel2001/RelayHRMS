@{
    ViewData["Title"] = "Employee Panel";
    Layout = "~/Areas/EmployeePanel/Views/Shared/_EmployeeLayout.cshtml";
    string baseUrl = ViewBag.BaseUrl;
    string apiBase = ViewBag.BaseUrlAPI;
}
<link href="~/commoncss.css" rel="stylesheet" />

<style>
    .fixed-label {
        min-width: 180px;
        white-space: nowrap;
        margin-bottom: 0;
        text-align: right;
    }
</style>
<div class="card">
    <div class="card-header bg-transparent ml-0 py-0">
        <div class="row">
            <div class="col-6">
                <h4 class="pt-2 mb-0">
                    Comp Off Approval
                </h4>
            </div>


        </div>

        <div class="card">
            <div class="card-header bg-transparent">
                <div class="row align-items-center">
                    <div class="col">
                        <h6 class="mb-0">Search Panel</h6>
                    </div>

                </div>
            </div>

            <div class="card-body">
                <form>
                    <div class="col-md-8 mx-auto">

                        <div class="row mb-3">
                            <!-- Left: Search Type -->
                            <div class="col-md-6 d-flex align-items-center">
                                <label for="searchtypeid" class="fixed-label">Search Type:</label>
                                <select class="form-control form-control-sm" id="searchtypeid">
                                    <option value="0">Employee Name</option>
                                    <option value="1">Emp Code</option>
                                </select>
                            </div>

                            <!-- Right: Search For -->
                            <div class="col-md-6 d-flex align-items-center">
                                <label for="searchforid" class="fixed-label">Search For:</label>
                                <input type="text" class="form-control form-control-sm" id="searchforid" />
                            </div>
                        </div>


                        <!-- Status Radio Buttons -->
                        <div class="row mb-3 justify-content-center">
                            <div class="col-md-4 d-flex justify-content-center gap-4">
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="Status" id="pendingid">
                                    <label class="form-check-label" for="pendingid">Pending</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="Status" id="approvedid">
                                    <label class="form-check-label" for="approvedid">Approved</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="radio" name="Status" id="rejectedid">
                                    <label class="form-check-label" for="rejectedid">
                                        Rejected
                                    </label>
                                </div>
                            </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="row mb-3 justify-content-center">
                            <div class="col-md-6 text-center">
                                <button type="button" id="goid" class="btn btn-primary btn-sm me-2">GO</button>
                                <button type="button" id="clearid" class="btn btn-secondary btn-sm">Clear</button>
                            </div>
                        </div>

                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<script>
     const apiBase = '@apiBase';
    const Empid = parseInt(localStorage.getItem("EmployeeId"));

    $(document).ready(function () {
        
        
         
           const status = $('input[name="Status"]:checked').val();
    });



    function loadreportingperson() {
        alert("Hii")
      $.ajax({
    const url = `${apiBase}/CompOffAPI/ReportingPersonEmpVise?Empid=${Empid}&status=${status}`;
        method: 'POST',

        success: function (res) {
            debugger;
            console.log("res",res);
             if (res.) {
                
            }  else {
                alert("⚠ " + res.responseMessage);
            }
        },
        error: function (xhr) {
            alert("❌ Server error: " + xhr.responseText);
        }

        });

    }
</script>

                        
